{{ if or .Values.waitForMetalNode .Values.setDefaultVirtStorageClass }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
{{ end }}
